#include<stdio.h>

int main(){
    //if condition is ture the output is 1 and false is 0
    // Q1 8^8 is valid statement pr not
    //  printf("%d \n", 8 ^ 8); // true value then  statement is valid.
    //  Q2 int=x; int y=x is valid or not?
    //  int x =5;
    //  int y = x;
    //  printf("%d \n ", y); // this condition is ture then statement is valid.
    // Q3 int x; y=x is valid or not
    //int x,y = x; this statement is in valid due for y is nor define .
    // char star = "**";
    // printf("%c \n", star); //this statement is invalid due for multiple character.
    return 0;
}