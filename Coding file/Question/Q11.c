#include<stdio.h>

int main(){
    int x = 2;
    if(x=1){
        printf("x is equal to 1"); // conditon is true because only assign the value not checking
    }else{
        printf("x is not equal to 1");
    }
    return 0;
}