#include<stdio.h>
#include<math.h>

int main(){
    printf("%d \n", 4 != 4);// due for the exlamation sign then condition is  output is zero because exlamation change the value positive change into negitive and negtive change into positive.
    printf("%d \n", 4 == 4);//this value is equal to the value then output is 1
    printf("%d \n", 4 != 3);// due for exlamation sign the condition is ture .
    printf("%d \n",4==3);// this condition is false because 4 is not equal to 3.
    return 0;
}