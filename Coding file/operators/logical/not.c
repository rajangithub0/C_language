#include<stdio.h>

int main(){
    printf("%d \n", !(3 > 4)); // the output is ture not operator is change the expression value condition is ture then output is false if condtion is false then output is ture
    printf("%d \n", !(5 > 1)); // the output is false because condition is ture
    return 0;
}